# HEARTBEAT.md

# aria does not use heartbeats this milestone.
# all recurring work is driven by cron jobs (see `openclaw cron list`).
# this file is intentionally empty so the heartbeat API calls are skipped.
